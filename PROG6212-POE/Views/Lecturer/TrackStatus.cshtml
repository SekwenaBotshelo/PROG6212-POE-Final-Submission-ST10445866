@model IEnumerable<PROG6212_POE.Models.Claim>
@{
    ViewData["Title"] = "Track Claim Status";
}

<div class="container mt-4">
    <h1 class="mb-4 text-center">Track Claim Status</h1>

    @if (!Model.Any())
    {
        <div class="alert alert-info text-center">No claims submitted yet.</div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover table-bordered align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Claim ID</th>
                        <th>Lecturer</th>
                        <th>Month</th>
                        <th>Total Hours</th>
                        <th>Hourly Rate</th>
                        <th>Total Amount</th>
                        <th>Status</th>
                        <th>Progress</th>
                        <th>Supporting Documents</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var claim in Model)
                    {
                        var progress = claim.Status switch
                        {
                            "Pending Verification" => 33,
                            "Verified" => 66,
                            "Approved" => 100,
                            "Rejected" => 100,
                            _ => 0
                        };

                        var badgeClass = claim.Status switch
                        {
                            "Pending Verification" => "bg-warning",
                            "Verified" => "bg-info",
                            "Approved" => "bg-success",
                            "Rejected" => "bg-danger",
                            _ => "bg-secondary"
                        };

                        var progressClass = claim.Status switch
                        {
                            "Approved" => "bg-success",
                            "Rejected" => "bg-danger",
                            _ => "bg-info"
                        };

                        <tr class="@(claim.Status == "Pending Verification" ? "table-warning" : claim.Status == "Rejected" ? "table-danger" : "")">
                            <td>@claim.ClaimId</td>
                            <td>@claim.SafeLecturerName</td> <!-- Fixed: Use SafeLecturerName -->
                            <td>@claim.Month</td>
                            <td>@claim.TotalHours</td>
                            <td>R @claim.StoredHourlyRate</td> <!-- Fixed: Use StoredHourlyRate -->
                            <td>R @claim.StoredTotalAmount</td> <!-- Fixed: Use StoredTotalAmount -->
                            <td>
                                <span class="badge @badgeClass" title="@claim.Status">@claim.Status</span>
                            </td>
                            <td>
                                <div class="progress" style="height: 20px;">
                                    <div class="progress-bar @progressClass" role="progressbar"
                                         style="width:@progress%;"
                                         aria-valuenow="@progress" aria-valuemin="0" aria-valuemax="100"
                                         title="Progress: @progress%">
                                        @claim.Status
                                    </div>
                                </div>
                            </td>
                            <td>
                                <!-- FIXED: Use DocumentPath instead of SupportingDocuments -->
                                @if (!string.IsNullOrEmpty(claim.DocumentPath))
                                {
                                    <div class="text-truncate" style="max-width: 200px;">
                                        <a href="/uploads/@claim.DocumentPath" target="_blank" title="@claim.DocumentOriginalName">
                                            @claim.DocumentOriginalName
                                        </a>
                                    </div>
                                }
                                else
                                {
                                    <span class="text-muted">No documents</span>
                                }
                            </td>
                            <td>
                                <form asp-action="UploadDocuments" asp-route-claimId="@claim.ClaimId"
                                      method="post" enctype="multipart/form-data" class="d-flex gap-2">
                                    <input type="file" name="supportingFile" class="form-control form-control-sm" />
                                    <button type="submit" class="btn btn-primary btn-sm" title="Upload supporting document">
                                        Upload
                                    </button>
                                </form>
                                <!-- FIXED: Document count display -->
                                @if (!string.IsNullOrEmpty(claim.DocumentPath))
                                {
                                    <small class="text-muted">Document attached</small>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <!-- Optional Guidance Card -->
    <div class="card shadow-sm mt-4">
        <div class="card-body">
            <h5 class="card-title mb-3">Guidance</h5>
            <ul class="text-muted">
                <li>Use the "Upload" button to add supporting documents directly from this dashboard.</li>
                <li>Status badges indicate current claim progress; the progress bar gives a visual representation.</li>
                <li>Ensure all documents are in allowed formats: .pdf, .docx, .xlsx.</li>
                <li>Pending or Rejected claims are highlighted for attention.</li>
            </ul>
        </div>
    </div>
</div>